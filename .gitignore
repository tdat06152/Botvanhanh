# ... (giữ nguyên các dòng cũ nếu có thể, nhưng tool write_to_file sẽ ghi đè)
# Để an toàn, tôi sẽ append vào cuối hoặc viết lại đầy đủ. 
# Vì tool này ghi đè, tôi sẽ viết lại nội dung cũ + thêm dòng mới.

# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
.env

# Google Cloud Keys (QUAN TRỌNG)
service-account.json
*.json.key

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
